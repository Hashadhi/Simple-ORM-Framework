/**
 *
 * A simple ORM framework that creates a database and its tables given the tables by means of classes with <code>@Entity</code> annotation and primary key by means of <code>@Id</code> annotation
 *
 * A valid JDBC connection is to be given in order for the framework to connect with JDBC
 *
 *
 * @author Hashadhi-Jayasinghe
 * @since 1.0.0
 */

package lk.ijse.dep8.orm;